<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/mainAppColor</item>
        <item name="colorPrimaryDark">@color/mainAppColor</item>
        <item name="colorAccent">@color/mainAppColor</item>
    </style>
    <style name="MySearchView" parent="Widget.AppCompat.SearchView">
        <item name="queryHint">@string/find_by_ingredients</item>
        <item name="android:textColor">@color/greyText</item>
    </style>
    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>
    <style name="exitAnimTheme" parent="AppTheme.NoActionBar">
        <item name="android:windowAnimationStyle">@style/MyAnimation.Exit</item>
    </style>
    <style name="MyAnimation.Exit" parent="@android:style/Animation.Dialog">
        <item name="android:windowExitAnimation">@anim/exit</item>
    </style>
    <style name="MyAnimation.Window" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/enter</item>
        <item name="android:windowExitAnimation">@anim/exit</item>
    </style>
    <style name="alphaAnimTheme" parent="AppTheme">
        <item name="android:windowAnimationStyle">@style/MyAnimation.Window</item>
    </style>

</resources>
